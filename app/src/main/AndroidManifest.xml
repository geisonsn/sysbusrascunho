<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.gsn.sysbusrascunho" >

    <uses-sdk android:minSdkVersion="3" android:maxSdkVersion="22"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar" >

        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name" >

        </activity>

        <activity android:name=".activities.InicialActivity">
        </activity>

        <activity android:name=".activities.NovoUsuarioActivity">
        </activity>

        <activity android:name=".activities.NovaReclamacaoActivity">
        </activity>

        <activity android:name=".activities.NovaLinhaActivity">
        </activity>

        <activity android:name=".activities.CheckinActivity">
        </activity>

        <activity android:name=".activities.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
